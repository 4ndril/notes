# After Install
sudo pacman -S git linux-headers pacman-contrib

# Fastfetch
sudo pacman -S fastfetch

# Ghostty
sudo pacman -S ghostty

# ProtonVPN
sudo pacman -S proton-vpn-gtk-app

# Transmission
sudo pacman -S transmission-gtk

# OpenCode
curl -fsSL https://opencode.ai/v2/install | bash

# Zed
sudo pacman -S zed


